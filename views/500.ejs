<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Error - Budget Claims System</title>
    <link rel="stylesheet" href="/css/styles.css">
</head>
<body>
    <div class="error-container">
        <h1>500 - Internal Server Error</h1>
        <% if (typeof message !== 'undefined' && message) { %>
            <p class="error-message"><%= message %></p>
        <% } else { %>
            <p class="error-message">An unexpected error occurred. Please try again later.</p>
        <% } %>
        <div class="button-group">
            <a href="javascript:history.back()" class="btn">Go Back</a>
            <a href="/" class="btn">Home</a>
        </div>
    </div>

    <% if (process.env.NODE_ENV === 'development') { %>
        <div class="debug-info">
            <p>Debug Information (Development Only):</p>
            <pre><%= JSON.stringify({ message, stack: (typeof stack !== 'undefined' ? stack : '') }, null, 2) %></pre>
        </div>
    <% } %>
</body>
</html>