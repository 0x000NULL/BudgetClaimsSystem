<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Customer Login - Budget Claims System</title>
    <link rel="stylesheet" href="/css/styles.css">
</head>
<body>
    <header>
        <!-- Budget logo and page title -->
        <img src="/images/Budget_logo.svg" alt="Budget Logo" class="logo">
        <h1>Budget Claims System</h1>
    </header>
    <main>
        <h2>Customer Login</h2>

        <!-- Login form -->
        <form action="/customer/login" method="POST">
            <% if (error && error.length > 0) { %>
                <div class="error-message"><%= error[0] %></div>
            <% } %>
            <div>
                <label for="email">Email:</label>
                <input type="email" id="email" name="email" required>
            </div>
            <div>
                <label for="password">Password:</label>
                <input type="password" id="password" name="password" required>
            </div>
            <button type="submit">Login</button>
        </form>
        <p>Don't have an account? <a href="/customer/register">Register here</a></p>
        <p>Are you an employee? <a href="/login">Employee Login</a></p>
    </main>
</body>
</html>
